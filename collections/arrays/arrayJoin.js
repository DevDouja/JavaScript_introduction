/**
 * Array.prototype.join
 * This is the exact opposite of .split(). Instead of turning a string into an array, we are turning an array into a string! The parameter is what the items will be separated by in the string.
*/
var myArray = ['Dashes', 'are', 'awesome','!'];
console.log( myArray.join('-'));